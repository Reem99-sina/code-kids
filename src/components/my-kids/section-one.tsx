import { MyKidsIcon } from "@/assets";

const SectionOne = () => {
  return (
    <section className="bg-[url('/main-parent.png')] min-h-[452px] w-full bg-repeat-round">
      <div className="flex items-center justify-start container mx-auto">
        <MyKidsIcon />
      </div>
    </section>
  );
};

export default SectionOne;
